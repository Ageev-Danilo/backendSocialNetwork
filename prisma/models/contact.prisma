model Contact {
  id             Int @id @default(autoincrement())

  contactOwnerId Int
  contactOwner   Profile @relation("OwnerContacts", fields: [contactOwnerId], references: [id])

  contactUserId  Int
  contactUser    Profile @relation("UserContacts", fields: [contactUserId], references: [id])
}